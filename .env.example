# Optional: keeps the existing Supabase enquiry log working.
# EmailJS delivery is built into the site and does not require environment variables.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
